import orm.models as models
import orm.fields as fields
class SaleOrder(models.Model):
    _name = 'sale.order'

    _fields = {}
    _fields['name'] = fields.Char(string="Sale Order", required=True)

sale = SaleOrder()
record = sale.read()
record.name = 'Test'
sale.update(record)
print(record)
