import orm.models as models
import orm.fields as fields
class SaleOrder(models.Model):
    _name = 'sale.order'

    name = fields.Char(string="Sale Order", required=True)

sale = SaleOrder()
sale.name = 2
sale2 = sale.env['sale.order'].browse()
console.log(sale2)
