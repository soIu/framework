import orm.models as models
import web.react as react

div = react.get_framework_components('div')

def Header(props):
    style = {'margin': 0, 'backgroundColor': '#8F8F8F', 'minHeight': '56px'}
    invisible = props.invisible
    if v'props.invisible instanceof Function':
       invisible = props.invisible(models.env.context.active_id)
    if invisible: style.display = 'none'
    return div(style=style, props.children)

react.register_component(Header)
